---
title: "Introduction à JavaScript"
description: "Découvrez les bases de JavaScript, son histoire et sa syntaxe"
---

import Rendu from "../../../components/rendu.astro";

# JavaScript : Les bases

## 1. Un peu d'histoire

JavaScript a été créé en 1995 par Brendan Eich alors qu'il travaillait chez Netscape Communications. À l'origine, il devait s'appeler LiveScript, mais a été renommé JavaScript pour surfer sur la popularité du langage Java à l'époque.

Le langage a été développé en seulement 10 jours pour répondre à un besoin crucial : ajouter de l'interactivité aux pages web. À cette époque, les pages HTML étaient statiques et il fallait recharger la page pour chaque interaction.

Quelques dates clés :

- 1995 : Création de JavaScript
- 1996 : Standardisation par ECMA International (d'où le nom ECMAScript)
- 2009 : Node.js est créé, permettant d'exécuter JavaScript côté serveur
- 2015 : ECMAScript 6 (ES6) apporte des améliorations majeures au langage

## 2. Premiers pas avec JavaScript

### Dans le navigateur

Pour exécuter du JavaScript dans une page web, vous pouvez utiliser la balise `<script>` :

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Mon premier JavaScript</title>
  </head>
  <body>
    <script>
      console.log("Bonjour depuis JavaScript !");
    </script>
  </body>
</html>
```

Afin de visualiser le résultat, il faut enregistrer le fichier et l'ouvrir avec le navigateur Chrome que nous avons installé en cours de route.

Plusieurs possibilités s'offrent à vous :

1. Ouvrir le fichier index.html dans le navigateur via le menu contextuel (Fichier > Ouvrir le fichier)
2. Utiliser la fonctionnalité "Show Preview (External Browser)" de Visual Studio Code, qui permet d'ouvrir le fichier dans le navigateur "par défaut". \
   Cette option provient de l'extension "Live Preview" que nous avons installée en cours.
   ![Show Preview (External Browser)](../../../assets/live-preview-in-browser.png)

Une fois le fichier ouvert, il vous sera possible d'afficher la console du navigateur en appuyant sur `Ctrl+Shift+I` (ou `Cmd+Option+I` sur Mac) afin d'ouvrir les outils de développement.
![console de Chrome](../../../assets/console-chrome.png)

**BONUS**: il est également possible de "déboguer" l'exécution du code en plaçant des points d'arrêt dans le code et en lançant le débogage dans la console.
Encore une fois, deux options s'offrent à vous :

1. Utiliser directement les outils de Chrome: choisir l'onglet "Sources" et placer un point d'arrêt en cliquant sur la ligne de code de votre choix.
   Puis rechargez la page et le point d'arrêt sera atteint:
   ![console de Chrome](../../../assets/debug-chrome.png)
2. Utiliser directement l'extension "Live Preview" de Visual Studio Code, que nous avons installée en cours: placez votre breakpoint puis utiliser la commande "Show Debug Preview (External Browser)"
   ![debug dans VSCode](../../../assets/debug-live.png)
   vous pourrez alors voir le débogage se lancer dans le navigateur:
   ![breakpoint dans VSCode](../../../assets/vscode-break.png)

### Avec Node.js

Vous pouvez aussi exécuter JavaScript en dehors du navigateur avec Node.js. Créez un fichier `script.js` :

```javascript
console.log("Bonjour depuis Node.js !");
```

Puis exécutez-le dans le terminal :

```bash
node script.js
```

**BONUS**: il est également possible de lancer le script directement depuis VSCode en lançant le débogage avec Node.js.
pour cela, il suffit de placer un point d'arrêt dans le code et de lancer le débogage avec le bouton "Déboguer" (F5) dans VSCode, avec le fichier ouvert dans l'éditeur.

### Exercices - Premiers pas

1. Créez une page HTML qui affiche "Bonjour le monde !" dans la console.
2. Modifiez le code pour afficher la date actuelle dans la console en utilisant `console.log(new Date())`.
3. **Défi**: Utilisez le débogueur pour examiner la valeur de `new Date()` en plaçant un point d'arrêt.

## 3. Les bases de la syntaxe

### Les variables

En JavaScript moderne, nous utilisons `let` et `const` pour déclarer des variables :

```javascript
// let pour les variables qui peuvent changer
let age = 25;
age = 26; // OK

// const pour les constantes
const nom = "Alice";
// nom = "Bob"; // Erreur !
```

### Les types de données

JavaScript possède plusieurs types de base :

```javascript
// Nombres
let nombre = 42;
let decimal = 3.14;
// ATTENTION : En JavaScript, tous les nombres sont stockés en tant que nombres à virgule flottante double précision (64 bits)
// Il n'y a pas de distinction entre entiers et décimaux comme dans d'autres langages !

// Chaînes de caractères
let texte = "Bonjour";
let texte2 = "Monde";

// comme pour python avec les "format strings", il est possible d'utiliser des variables dans les chaînes de caractères via une syntaxe similaire
let texte3 = `Bonjour ${texte} et ${texte2} !`;

// Booléens
let estVrai = true; // attention, true et false sont des mots réservés en JavaScript et ne prennent pas de majuscules !
let estFaux = false;

// Tableaux
let fruits = ["pomme", "banane", "orange"];

// Objets
let personne = {
  nom: "Alice",
  age: 25,
};

// Undefined et null
let nonDefini;
let vide = null;
```

### Exercices - Types et Variables

1. Déclarez trois variables :
   - Une constante avec votre nom
   - Une variable avec votre âge
   - Un tableau avec vos couleurs préférées
2. Créez un objet qui décrit un animal avec au moins 3 propriétés
3. **Défi**: Utilisez un template string pour créer une phrase qui utilise toutes vos variables

## 4. Les fonctions

Voici un exemple simple de fonction qui calcule l'aire d'un rectangle :

```javascript
function calculerAire(longueur, largeur) {
  return longueur * largeur;
}

// Utilisation de la fonction
let aire = calculerAire(5, 3);
console.log("L'aire est de : " + aire); // Affiche 15
```

On remarquera les éléments de cette syntaxe:

- `function` : mot réservé pour définir une fonction
- `calculerAire` : nom de la fonction
- `(longueur, largeur)` : les paramètres de la fonction
- `return` : mot réservé pour renvoyer une valeur (comme en Python !)
- `let aire = calculerAire(5, 3);` : appel de la fonction avec les arguments 5 et 3

**BONUS**: il est également possible de définir des fonctions en une seule ligne, comme en Python avec `lambda`:

```javascript
let aire = (longueur, largeur) => longueur * largeur;
```

Cette syntaxe est très pratique pour les fonctions simples qui ne nécessitent qu'une seule instruction.
Les éléments de cette syntaxe sont:

- `(longueur, largeur)` : les paramètres de la fonction
- `=>` : mot réservé pour définir une fonction en une seule ligne
- `longueur * largeur` : l'instruction à exécuter

Il est même possible de mélanger les deux syntaxes, en utilisant la syntaxe `=>` et en l'associant à un bloc d'instructions encadré par des accolades `{}` par exemple:

```javascript
let aire = (longueur, largeur) => {
  return longueur * largeur;
};
```

### Exercices - Fonctions

1. Écrivez une fonction qui convertit des euros en dollars (1 euro = 1.09 dollars)
2. Créez une fonction fléchée qui calcule le carré d'un nombre

## 5. Les structures de contrôle

### Conditions (if)

```javascript
let age = 18;

if (age >= 18) {
  console.log("Vous êtes majeur");
} else {
  console.log("Vous êtes mineur");
}
```

### Boucles

La boucle for classique :

```javascript
for (let i = 0; i < 5; i++) {
  console.log("Tour numéro " + i);
}
```

Les boucles modernes pour les tableaux :

```javascript
const fruits = ["pomme", "banane", "orange"];

// for...of : parcourt les valeurs
for (const fruit of fruits) {
  console.log(fruit); // affiche: pomme, banane, orange
}

// for...in : parcourt les indices
for (const index in fruits) {
  console.log(`${index}: ${fruits[index]}`); // affiche: 0: pomme, 1: banane, 2: orange
}

// forEach : méthode avec fonction callback
fruits.forEach((fruit, index) => {
  console.log(`${index}: ${fruit}`);
});
```

La boucle while :

```javascript
let compteur = 0;
while (compteur < 3) {
  console.log("Compteur : " + compteur);
  compteur++;
}
```

### Exercices - Structures de contrôle

1. Écrivez une boucle qui affiche les nombres de 1 à 10
2. Créez une condition qui vérifie si un nombre est pair ou impair
3. **Défi**: Écrivez une boucle qui affiche la table de multiplication d'un nombre

## 6. Les Classes en JavaScript

### Introduction aux classes

Les classes ont été introduites avec ES6 (2015) pour faciliter la programmation orientée objet en JavaScript. Elles permettent de créer des "modèles" pour nos objets :

```javascript
class Personne {
  constructor(nom, age) {
    this.nom = nom;
    this.age = age;
  }

  sePresenter() {
    return `Je m'appelle ${this.nom} et j'ai ${this.age} ans`;
  }
}

// Création d'une instance
const alice = new Personne("Alice", 25);
console.log(alice.sePresenter()); // "Je m'appelle Alice et j'ai 25 ans"
```

### Propriétés et méthodes

Les classes peuvent avoir :

- Des propriétés d'instance (définies dans le constructor)
- Des méthodes d'instance
- Des propriétés et méthodes statiques

```javascript
class Voiture {
  // Propriété statique
  static nombreDeRoues = 4;

  // Propriétés privées (avec #)
  #kilometrage = 0;

  constructor(marque, modele) {
    // Propriétés d'instance
    this.marque = marque;
    this.modele = modele;
  }

  // Méthode d'instance
  rouler(km) {
    this.#kilometrage += km;
    console.log(`La voiture roule sur ${km}km`);
  }

  // Getter
  get info() {
    return `${this.marque} ${this.modele} (${this.#kilometrage}km)`;
  }

  // Méthode statique
  static description() {
    return `Une voiture a ${this.nombreDeRoues} roues`;
  }
}

const maVoiture = new Voiture("Renault", "Clio");
maVoiture.rouler(100);
console.log(maVoiture.info); // "Renault Clio (100km)"
console.log(Voiture.description()); // "Une voiture a 4 roues"
```

### Héritage

Les classes peuvent hériter d'autres classes avec le mot-clé `extends` :

```javascript
class Animal {
  constructor(nom) {
    this.nom = nom;
  }

  faireDuBruit() {
    return "Un bruit quelconque";
  }
}

class Chat extends Animal {
  constructor(nom, couleur) {
    // appel du constructeur parent
    super(nom);
    this.couleur = couleur;
  }

  faireDuBruit() {
    return "Miaou !";
  }

  ronronner() {
    return "Prrrrr";
  }
}

const minou = new Chat("Minou", "roux");
console.log(minou.faireDuBruit()); // "Miaou !"
console.log(minou.ronronner()); // "Prrrrr"
```

### Points importants à retenir

1. Le `constructor` est appelé automatiquement lors de la création d'une instance
2. `this` fait référence à l'instance en cours
3. Les propriétés privées commencent par `#`
4. Les méthodes/propriétés statiques appartiennent à la classe, pas aux instances
5. `super()` permet d'appeler le constructeur de la classe parente
6. Les getters/setters permettent de contrôler l'accès aux propriétés

### Exemple pratique : Gestion d'une bibliothèque

```javascript
class Livre {
  #emprunte = false;

  constructor(titre, auteur) {
    this.titre = titre;
    this.auteur = auteur;
  }

  emprunter() {
    if (this.#emprunte) {
      return "Déjà emprunté";
    }
    this.#emprunte = true;
    return "Livre emprunté avec succès";
  }

  rendre() {
    if (!this.#emprunte) {
      return "Le livre n'était pas emprunté";
    }
    this.#emprunte = false;
    return "Livre rendu avec succès";
  }

  get statut() {
    return this.#emprunte ? "Emprunté" : "Disponible";
  }
}

class Bibliotheque {
  constructor() {
    this.livres = [];
  }

  ajouterLivre(livre) {
    this.livres.push(livre);
  }

  rechercherParTitre(titre) {
    return this.livres.find((livre) => livre.titre === titre);
  }
}

// Utilisation
const biblio = new Bibliotheque();
const livre1 = new Livre("Le Petit Prince", "Saint-Exupéry");
biblio.ajouterLivre(livre1);

console.log(livre1.statut); // "Disponible"
console.log(livre1.emprunter()); // "Livre emprunté avec succès"
console.log(livre1.statut); // "Emprunté"
```
